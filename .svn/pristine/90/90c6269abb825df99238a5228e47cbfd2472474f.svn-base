package com.xinyan.ssme.sys.mapper;

import org.springframework.transaction.annotation.Transactional;

import com.xinyan.ssme.sys.bean.SysUser;

/**
 * MaBatis 的 Mapper 接口
 * 1、不需要实现类
 * 2、一个 Mapper 接口对应一个 Mapper 映射文件
 * 3、Mapper 映射文件中指定 namespace 为 这个Mapper 的全类名
 * 4、Mapper 映射文件中的各个标签的 id 对应着 Mapper 接口中的 方法名。
 * @author Administrator
 *
 */
@Transactional
public interface SysMapper {

	//对应着 Mapper 接口中的一个 标签 id
	public SysUser getSysUser(SysUser sysUser);
}
